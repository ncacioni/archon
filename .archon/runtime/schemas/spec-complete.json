{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "spec-complete",
  "description": "Handoff from spec-writer to security: spec is ready for security review",
  "type": "object",
  "required": ["spec_file", "spec_type", "completed_at", "summary"],
  "properties": {
    "spec_file": {
      "type": "string",
      "description": "Path to the spec file (relative to project root)"
    },
    "spec_type": {
      "type": "string",
      "enum": ["openapi", "db-schema", "domain-model", "wireframe", "state-machine", "mixed"]
    },
    "completed_at": {
      "type": "string",
      "description": "ISO 8601 datetime"
    },
    "summary": {
      "type": "string",
      "description": "One-paragraph summary of what was specified"
    },
    "endpoints": {
      "type": "array",
      "items": { "type": "string" },
      "description": "API endpoints defined (for OpenAPI specs)"
    },
    "models": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Data models or domain entities defined"
    },
    "auth_required": {
      "type": "boolean",
      "description": "Whether the spec includes authentication/authorization requirements"
    },
    "external_services": {
      "type": "array",
      "items": { "type": "string" },
      "description": "External APIs or services this spec touches"
    }
  },
  "additionalProperties": false
}
