{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "implementation-complete",
  "description": "Handoff from builder to qa: implementation is ready for quality assurance",
  "type": "object",
  "required": ["completed_at", "summary", "files_changed"],
  "properties": {
    "completed_at": {
      "type": "string",
      "description": "ISO 8601 datetime"
    },
    "summary": {
      "type": "string",
      "description": "What was implemented and key decisions made"
    },
    "files_changed": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of files created or modified"
    },
    "spec_file": {
      "type": "string",
      "description": "Spec file this implementation fulfills"
    },
    "architecture_layer": {
      "type": "string",
      "enum": ["domain", "application", "adapter", "full-stack", "infrastructure"],
      "description": "Which Clean Architecture layer(s) were touched"
    },
    "test_files": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Test files written as part of implementation"
    },
    "known_gaps": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Known gaps or deferred items for QA to be aware of"
    },
    "security_conditions_met": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Security approval conditions that were addressed"
    },
    "dependencies_added": {
      "type": "array",
      "items": { "type": "string" },
      "description": "New npm/pip/etc packages added"
    }
  },
  "additionalProperties": false
}
